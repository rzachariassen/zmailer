#
#  One of test configurations without root powers at the machine
#  where the system was tested at :-)
#
./configure --prefix=$HOME/mail --with-system-malloc --with-logdir=$HOME/log --with-zmailer-conf=$HOME/mail/zmailer.conf --with-mailbox=$HOME/mailspool --with-postoffice=$HOME/postoffice
